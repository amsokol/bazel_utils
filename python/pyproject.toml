[project]
name = "bazel-utils"
version = "0.1.0"
description = "Starlark helpers; pip-audit is pinned here for consumers."
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "build==1.6.1",
    "pip-audit==2.10.1",
    "setuptools==84.0.0",
    "typing-extensions==4.16.0",
    "wheel==0.48.0",
]
