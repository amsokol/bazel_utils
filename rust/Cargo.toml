[package]
name = "bazel-utils"
version = "0.1.0"
edition = "2021"
publish = false
description = "Pins cargo-audit for crate_universe; not an application crate."

[lib]
path = "lib.rs"

[dependencies]
cargo-audit = { version = "=0.22.2", default-features = false }
