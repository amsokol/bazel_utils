"""Prebuilt cargo-audit from GitHub; cargo -V from the consumer rules_rust toolchain."""

module(
    name = "bazel_utils_rust",
    version = "0.2.2",
)

bazel_dep(name = "bazel_skylib", version = "1.9.2")
bazel_dep(name = "bazel_utils_core", version = "0.2.2")
bazel_dep(name = "rules_rust", version = "0.73.0")

include("//:cargo_audit.MODULE.bazel")
