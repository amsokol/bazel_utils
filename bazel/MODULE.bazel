"""Prebuilt buildifier from GitHub releases."""

module(
    name = "bazel_utils_bazel",
    version = "0.2.9",
)

bazel_dep(name = "bazel_skylib", version = "1.9.2")
bazel_dep(name = "bazel_utils_core", version = "0.2.9")

include("//:buildifier.MODULE.bazel")
