[project]
name = "bazel-utils"
version = "0.1.0"
description = "Starlark helpers; ruff is pinned here for consumers."
requires-python = "==3.14"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = ["ruff==0.16.3"]
build = ["build==1.5.0", "setuptools==84.0.0", "wheel==0.48.0"]
